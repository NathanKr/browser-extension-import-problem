import {sum} from './utils1.js'; // --- using .js is a must because of dynamic import
import {mul} from './utils2.js'; // --- using .js is a must because of dynamic import

console.log(`sum(1,2) : ${sum(1,2)}`); 
console.log(`mul(1,2) : ${mul(1,2)}`); 
